# This will be deployed and run inside a TEE with cryptographic attestation.

services:
    openrank-computer:
        image: lazovicff/openrank-computer@sha256:a8b8169a9136518d0ed0db48c78ba307e994a08dffebb34fd876739de2c35c56
        environment:
            # Set your mnemonic here or use secrets management
            - RUST_LOG=info
            - MNEMONIC=${MNEMONIC}
            - OPENRANK_MANAGER_ADDRESS=${OPENRANK_MANAGER_ADDRESS}
            - CHAIN_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/${ALCHEMY_API_KEY}
            - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
            - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
        restart: unless-stopped

    # openrank-challenger:
    #     image: lazovicff/openrank-computer@sha256:a8b8169a9136518d0ed0db48c78ba307e994a08dffebb34fd876739de2c35c56
    #     command: ["--challenger"]
    #     environment:
    #         # Set your mnemonic here or use secrets management
    #         - RUST_LOG=info
    #         - MNEMONIC=${MNEMONIC}
    #         - OPENRANK_MANAGER_ADDRESS=${OPENRANK_MANAGER_ADDRESS}
    #         - CHAIN_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/${ALCHEMY_API_KEY}
    #         - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
    #         - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
    #     restart: unless-stopped
