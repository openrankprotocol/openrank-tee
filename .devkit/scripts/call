#!/usr/bin/env bash
set -e

# source in helper functions
source "$( dirname "${BASH_SOURCE[0]}" )/helpers/helpers.sh"

log "Error: Task submission is not implemented in this template"
log "This template only provides basic TEE Docker Compose deployment"
log "For task submission functionality, use a different AVS template"
exit 1 
